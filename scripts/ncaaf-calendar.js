// NCAAF Archive Calendar Data
// Last updated: December 25, 2025
// Auto-generated by sync_calendars.py - DO NOT EDIT MANUALLY

const ARCHIVE_DATA = [
    { date: "2025-12-24", page: "ncaaf.html", title: "NCAAF Analysis - December 24, 2025" },
    { date: "2025-12-24", page: "ncaaf-page8.html", title: "NCAAF Analysis Archive - Page 8" },
    { date: "2025-12-24", page: "ncaaf-page7.html", title: "NCAAF Analysis Archive - Page 7" },
    { date: "2025-12-24", page: "ncaaf-page6.html", title: "NCAAF Analysis Archive - Page 6" },
    { date: "2025-12-24", page: "ncaaf-page5.html", title: "NCAAF Analysis Archive - Page 5" },
    { date: "2025-12-24", page: "ncaaf-page4.html", title: "NCAAF Analysis Archive - Page 4" },
    { date: "2025-12-24", page: "ncaaf-page24.html", title: "NCAAF Analysis Archive - Page 24" },
    { date: "2025-12-24", page: "ncaaf-page23.html", title: "NCAAF Analysis Archive - Page 23" },
    { date: "2025-12-24", page: "ncaaf-page22.html", title: "NCAAF Analysis Archive - Page 22" },
    { date: "2025-12-24", page: "ncaaf-page21.html", title: "NCAAF Analysis Archive - Page 21" },
    { date: "2025-12-24", page: "ncaaf-page20.html", title: "NCAAF Analysis Archive - Page 20" },
    { date: "2025-12-24", page: "ncaaf-page2.html", title: "College Football Rivalry Week Nov 28 2025" },
    { date: "2025-12-24", page: "ncaaf-page19.html", title: "NCAAF Analysis Archive - Page 19" },
    { date: "2025-12-24", page: "ncaaf-page18.html", title: "NCAAF Analysis Archive - Page 18" },
    { date: "2025-12-24", page: "ncaaf-page13.html", title: "NCAAF Analysis Archive - Page 13" },
    { date: "2025-12-24", page: "ncaaf-page12.html", title: "NCAAF Analysis Archive - Page 12" },
    { date: "2025-12-24", page: "ncaaf-page11.html", title: "NCAAF Analysis Archive - Page 11" },
    { date: "2025-12-24", page: "ncaaf-page10.html", title: "NCAAF Analysis Archive - Page 10" },
    { date: "2025-12-15", page: "ncaaf-page9.html", title: "NCAAF Analysis - December 15, 2025" },
    { date: "2025-12-10", page: "ncaaf-page14.html", title: "NCAAF Analysis - December 10, 2025" },
    { date: "2025-12-08", page: "ncaaf-page15.html", title: "NCAAF Analysis - December 8, 2025" },
    { date: "2025-12-06", page: "ncaaf-page16.html", title: "NCAAF Analysis - December 6, 2025" },
    { date: "2025-12-04", page: "ncaaf-page17.html", title: "NCAAF Analysis - December 4, 2025" },
    { date: "2025-11-01", page: "ncaaf-page3.html", title: "College Football Picks - Week 1, November 1, 2025" },
];

const dateMap = {};
ARCHIVE_DATA.forEach(item => { if (!dateMap[item.date]) dateMap[item.date] = item; });

const pageToDateMap = {};
ARCHIVE_DATA.forEach(item => { pageToDateMap[item.page] = item.date; });

const currentPage = window.location.pathname.split('/').pop() || 'index.html';
const currentPageDate = pageToDateMap[currentPage] || null;

const months = new Set();
ARCHIVE_DATA.forEach(item => { const [y, m] = item.date.split('-'); months.add(y + '-' + m); });

const today = new Date();
const currentMonth = today.getFullYear() + '-' + String(today.getMonth() + 1).padStart(2, '0');
const todayStr = today.getFullYear() + '-' + String(today.getMonth() + 1).padStart(2, '0') + '-' + String(today.getDate()).padStart(2, '0');
months.add(currentMonth);

const sortedMonths = Array.from(months).sort().reverse();
const monthNames = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];

let displayMonth = currentMonth;
if (currentPageDate) { const [py, pm] = currentPageDate.split('-'); displayMonth = py + '-' + pm; }

function renderCalendar(yearMonth) {
    const [year, month] = yearMonth.split('-').map(Number);
    const yearEl = document.getElementById('cal-year');
    if (yearEl) yearEl.textContent = year;
    const firstDay = new Date(year, month - 1, 1).getDay();
    const daysInMonth = new Date(year, month, 0).getDate();
    const container = document.getElementById('calendar-days');
    if (!container) return;
    container.innerHTML = '';
    for (let i = 0; i < firstDay; i++) { const cell = document.createElement('div'); cell.className = 'cal-day empty'; container.appendChild(cell); }
    for (let d = 1; d <= daysInMonth; d++) {
        const dateStr = year + '-' + String(month).padStart(2, '0') + '-' + String(d).padStart(2, '0');
        const hasData = dateMap[dateStr];
        let classes = 'cal-day';
        if (dateStr === todayStr) classes += ' today';
        if (dateStr === currentPageDate) classes += ' current-page';
        if (hasData) classes += ' has-content';
        const cell = document.createElement('div');
        cell.className = classes;
        cell.textContent = d;
        if (hasData) { cell.title = hasData.title; cell.onclick = () => window.location.href = hasData.page; }
        container.appendChild(cell);
    }
}

function initSportCalendar() {
    const monthSelect = document.getElementById('month-select');
    if (monthSelect) {
        monthSelect.innerHTML = '';
        sortedMonths.forEach(m => {
            const [year, month] = m.split('-');
            const opt = document.createElement('option');
            opt.value = m;
            opt.textContent = monthNames[parseInt(month) - 1] + ' ' + year;
            if (m === displayMonth) opt.selected = true;
            monthSelect.appendChild(opt);
        });
        monthSelect.addEventListener('change', function() { renderCalendar(this.value); });
    }
    renderCalendar(displayMonth);
    const mobileSelect = document.getElementById('mobile-archive-select');
    if (mobileSelect) {
        mobileSelect.innerHTML = '<option value="">Select a date...</option>';
        ARCHIVE_DATA.forEach(item => {
            const opt = document.createElement('option');
            opt.value = item.page;
            const dateObj = new Date(item.date + 'T12:00:00');
            const dateLabel = dateObj.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });
            opt.textContent = dateLabel + ' - ' + item.title;
            if (item.page === currentPage) opt.selected = true;
            mobileSelect.appendChild(opt);
        });
        mobileSelect.addEventListener('change', (e) => { if (e.target.value) window.location.href = e.target.value; });
    }
}

if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initSportCalendar); } else { initSportCalendar(); }
